# Dependencies for RunPod handler avec GPT-OSS-20B

# PDF processing
PyPDF2==3.0.1
pdfplumber==0.10.3
pytesseract==0.3.10
Pillow==10.2.0

# LLM inference avec GPT-OSS support
# vLLM sera installé via uv avec la version spéciale gpt-oss
transformers==4.47.0
torch==2.5.1
accelerate==1.2.1

# OpenAI Harmony package (pour le format Harmony)
gpt-oss

# API
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.6.0

# Utilities
python-multipart==0.0.6
aiohttp==3.9.3
regex==2023.12.25
runpod==1.6.2

# Kernels pour GPT-OSS (optimisations)
kernels

